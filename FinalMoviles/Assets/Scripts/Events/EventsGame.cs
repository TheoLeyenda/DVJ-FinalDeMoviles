using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventsGame : MonoBehaviour
{
    // Start is called before the first frame update
    public EventOf eventOf;
    public enum EventOf
    {
        None,
        Tutorial,
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
